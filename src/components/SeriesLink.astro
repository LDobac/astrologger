---
import { slug as slugger } from "github-slugger";

interface Props {
    series: string
}

const { series } = Astro.props;
---

<a href={"/series/" + slugger(series) + "/1"} class="inline text-accent hover:text-fg transition">{series}</a>
